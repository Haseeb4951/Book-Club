<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookClubApplication"
        tools:targetApi="31">
        <activity
            android:name=".RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".HomepageActivity"></activity>
        <activity android:name=".DashboardActivity"></activity>
        <activity android:name=".CategoryAddActivity"></activity>
        <activity android:name=".AddPdfActivity"></activity>
        <activity android:name=".PdfListActivity"></activity>
        <activity android:name=".PdfViewerActivity"></activity>
        <activity android:name=".ClubsActivity"></activity>
        <activity android:name=".SettingsActivity"></activity>
        <activity android:name=".CreateClubsActivity"></activity>
    </application>

</manifest>